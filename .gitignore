node_modules/
dist/
*.log
.env
.env.*
!.env.example
coverage/
# Ключи подписи живут в томе, не в git (Задача 16, шаг 4).
keys/
*.pem
*.jwk
portal/dist/
devsite/dist/

# Сборка котлин-клиента (clients/android).
clients/android/build/
clients/android/.gradle/
clients/android/.kotlin/

# Отпечаток инкрементальной сборки tsc. Он менялся от каждого прогона
# проверок и попадал в чужие ветки как «изменение».
*.tsbuildinfo
