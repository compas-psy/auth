/**
 * Портал аккаунта как клиент OIDC.
 *
 * Портал ходит в публичное API тем же способом, что и продукты, —
 * ключом доступа. Отдельного внутреннего пути для наших собственных
 * интерфейсов не существует (CLAUDE.md), а cookie сессии SSO, принятая
 * в /v1, таким путём и была бы: пользоваться ей мог бы только наш
 * портал.
 *
 * Клиент ПУБЛИЧНЫЙ: он живёт в браузере, и секрет там сохранить негде —
 * его достанут из кода страницы. Безопасность держится на PKCE (S256) и
 * на том, что адрес возврата закреплён здесь.
 *
 * Значения названы в коде, а не только в базе: адрес возврата обязан
 * совпадать байт в байт с тем, куда уводит сам портал, иначе провайдер
 * откажет на возврате, а по экрану причину не понять.
 */
export const PORTAL_CLIENT = "account-portal";
export const PORTAL_REDIRECT = "https://auth.cmpas.ru/account/callback";
